"""Service layer package for the image processing component."""

# Services in this package are intentionally kept importable one-by-one.
# Avoid adding eager model initialization here; large ML models should stay lazy
# inside their concrete service classes.
